# ============================================================================
# Secret — PostgreSQL 비밀번호
#
# ⚠️ 학습용 더미 값. 프로덕션에서는 External Secrets Operator 사용.
# echo -n "seemedi" | base64  →  c2VlbWVkaQ==
# ============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secret
  namespace: seemedi-dev
type: Opaque
data:
  POSTGRES_PASSWORD: c2VlbWVkaQ==
