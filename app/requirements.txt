# ============================================================================
# requirements.txt — 앱이 의존하는 Python 패키지 목록
#
# 사용처: app/Dockerfile의 Stage 1(builder)에서 pip install로 설치된다.
#
# DevOps 관점: 버전을 == 로 "완전 고정"하는 이유
#   - 오늘 빌드하든 6개월 뒤에 빌드하든 같은 이미지가 나와야 한다 (재현성).
#   - >= 나 미지정 버전을 쓰면 어느 날 갑자기 상위 버전이 설치되어
#     "아무것도 안 바꿨는데 배포가 깨지는" 사고가 난다.
#   - 버전을 올릴 때는 이 파일을 의도적으로 수정 → CI 검증 → 배포 순서를 밟는다.
# ============================================================================

fastapi==0.115.12          # 웹 API 프레임워크 — app/src/main.py의 FastAPI 앱 본체
uvicorn[standard]==0.34.3  # ASGI 웹 서버 — Dockerfile의 CMD가 이것으로 앱을 구동.
                           # [standard]: uvloop 등 성능 확장 포함 옵션
pydantic==2.11.7           # 데이터 검증 라이브러리 — FastAPI의 요청/응답 모델 기반
pydantic-settings==2.9.1   # 환경 변수 → 설정 객체 매핑 — app/src/core/config.py에서 사용
